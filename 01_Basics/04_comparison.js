// console.log(2 > 1);
// console.log(null == 0);

// console.log("0" === "0");   // strict check, it will check the data types as well as the values

// console.log(undefined == 0);

// console.log(null == 0); // equality check does not convert null into 0

console.log(null >= 0); // while comparison operations change the null into 0, so the result will be true
// or console.log(null <= 0);